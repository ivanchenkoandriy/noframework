<div class="container mt-10">
    <div class="row">
        <div class="col-md-12">
            {% if result %}
                <div class = "alert {% if result.isSuccess() %}alert-success{% else %}alert-danger{% endif %}" role = "alert">{{ result.getMessage() }}</div>
            {% endif %}
        </div>
    </div>
</div>